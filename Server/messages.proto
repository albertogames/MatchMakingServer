package messages;

enum GameState {
	DISP_PLAY = 0;
	DISP_PLAY_CANCEL = 1;
	LOG_OK = 2;
	LOG = 3;
	NEW_PLAYER_DISP_PLAY = 4;
	NEW_PLAYER_DISP_PLAY_CANCEL = 5;
}
  
message login{
	required GameState gameState = 1 [default = LOG];
	required string user = 2;
	required string password = 3;
}